// src/dep.ts
import { getPackageInfoSync } from "local-pkg";
var detectVueVersion = () => {
  const vuePkg = getPackageInfoSync("vue");
  if (vuePkg) {
    return +vuePkg.version.slice(0, 1);
  } else {
    return 3;
  }
};

export {
  detectVueVersion
};
